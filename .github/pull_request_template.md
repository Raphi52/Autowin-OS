<!-- 1 PR = 1 sujet. Une PR qui mélange deux features est refusée : c'est ce qui crée les collisions. -->

## Ce que ça change
<!-- En une ou deux phrases : le problème réel, pas la liste des fichiers. -->

## Preuve de vérification
<!-- OBLIGATOIRE : un artefact hors-modèle, pas une auto-déclaration.
     Ex : `npm test` → 1276 tests verts · `npm run typecheck` → exit 0 · capture lue · run réel observé.
     Un correctif de bug : nommer le rouge→vert (l'assertion qui échouait avant le fix). -->

- [ ] `npm run typecheck` exit 0
- [ ] `npm test` vert (préciser le nombre / les exclusions connues)
- [ ] Comportement observé en vrai si la logique change (capture, run, requête)

## Périmètre
<!-- Ce que la PR NE touche PAS. Aucun renommage/refactor « tant qu'on y est ». -->

## Points d'attention pour la relecture
<!-- Décisions délibérées, compromis, dette assumée. Ce qu'il ne faut PAS re-litiger. -->
