TACHE (identique pour les deux bras de ce duel) :
Dans CE dossier, `src/renderer/src/components/ChatComposer.tsx` porte 3 erreurs eslint
`react-hooks/refs` qui rendent `npm run lint` — donc `npm test` — rouge :
  - ligne 295 : `dicteeGainRef.current = dicteeGain` est ecrit EN PLEIN RENDU ;
  - lignes 449 et 474 : `acceptMention(c)` / `acceptSlash(c)` sont vus comme appeles pendant le
    rendu (ils vivent dans une fonction immediatement invoquee au milieu du JSX).
Corrige la CAUSE : la synchronisation du ref sort du rendu, et les deux listes deroulantes
cessent d appeler ces fonctions depuis une IIFE de rendu. Le comportement visible ne change pas.

CRITERE DE SUCCES — le seul qui compte, deterministe, AUJOURD HUI ROUGE :
  node C:/Sources/AutoWinOS/.autowin-data/autowin-os/worktrees/846afeda47fa9e64/agent__run-e4466f8bc37f-1/bench/check-d3.mjs .
doit afficher "CRITERE ATTEINT" avec le code de sortie 0.
Etat de depart (mesure) : G1 RATE (3 erreurs eslint : 295:5, 449:25, 474:25), G2/G3/G4 deja OK.
ATTENTION : une directive `eslint-disable` ou `@ts-ignore` est refusee par G2, et retirer le
reglage de volume est refuse par G3. G4 rejoue les tests du composant.

REGLES DURES (identiques pour tous les bras) :
- Tu travailles UNIQUEMENT dans ce dossier. Tu ne touches a aucun autre depot ni copie.
- Tu ne modifies PAS le fichier check-dX.mjs du critere (il vit hors de ce dossier, son empreinte
  est figee et re-verifiee apres coup : le modifier se voit et annule ton resultat).
- Tu ne fais aucun commit, aucun push, aucune commande git qui ecrit. Tu ne lances pas
  l application. Tu ne lances PAS la suite de tests complete (292 s, elle tue des workers).
- Correctif MINIMAL : pas de refactor opportuniste, pas de renommage de confort.
- Interdit : desactiver une regle, relacher une assertion, supprimer un test, avaler une erreur.

RENDS A LA FIN, en clair : (1) la liste ordonnee de ce que tu as fait, (2) la sortie exacte du
critere de succes avec son code de sortie, (3) les fichiers que tu as modifies, (4) ce qui a
echoue en route et comment tu t en es sorti.

WORKFLOW IMPOSE (X) — BUILD DIRECT + VERIFICATION CIBLEE. Interdiction d ecrire un plan, un
cadrage, une liste d options ou une procedure : tu vas DROIT au correctif. Boucle unique :
(1) tu lis le strict necessaire, (2) tu appliques le plus petit correctif que tu crois juste,
(3) tu relances IMMEDIATEMENT le seul critere de succes ci-dessus et tu LIS sa sortie,
(4) rouge -> tu corriges et tu reboucles ; vert -> tu t arretes. Aucun autre test, aucune autre
verification, aucune prose intermediaire. Si tu reboucles 5 fois sans vert, tu dis "bloque" et tu
rends la sortie du critere telle quelle.
