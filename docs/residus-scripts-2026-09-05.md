<!-- Provenance : livrable du bras X du banc /arena sur /residus (2026-09-05), recupere avant
     nettoyage de sa copie de travail. Retenu parmi 4 shortlists concurrentes (A: 8 entrees,
     B: 9, C: 6, X: 15) parce qu'il couvre le plus de candidats ET documente ses faux positifs.
     Verifie a la recuperation : les 15 fichiers candidats existent tous, et ceux marques
     'sans appelant' n'ont effectivement 0 reference dans le depot. Lecture seule : aucun
     fichier n'a ete supprime. -->

# Shortlist — code résiduel inutile de `scripts/`

Périmètre : les 159 fichiers de `scripts/` du dépôt. Lecture seule, rien n'a été supprimé, modifié ni renommé.

**Méthode (sans sonde).** Recensement manuel des références : lecture du contenu des 2 338 fichiers suivis
par git (hors `package-lock.json`) et recherche de chaque nom de fichier de `scripts/`, avec et sans
extension. Puis, sur les candidats sans appelant, vérification d'ouverture : résolution de chaque import
relatif, existence de chaque chemin `src/…` cité, existence de chaque chemin absolu codé en dur, présence
de chaque `data-testid` dans `src/`, et présence de chaque `window.api.<méthode>` dans `src/preload/`.
Les fichiers cités par `package.json`, `electron-builder.yml`, `vitest.config.ts` ou un autre script ont
été écartés (section suivante).

## Table

| Catégorie | file:line | Pourquoi c'est mort | Preuve d'absence d'appelant | Signal de retrait |
|---|---|---|---|---|
| Import vers un module supprimé | `scripts/probe-orchestrator-e2e.mts:4` | Importe `../src/main/authority/sas` ; le dossier `src/main/authority/` n'existe plus (supprimé par `75210ccf`, 2026-08-09). Le fichier ne peut plus se charger, quel que soit l'appelant. | Aucune occurrence de `probe-orchestrator-e2e` hors du fichier lui-même. Le symbole `AuthoritySas` n'existe plus que dans ce fichier, dans tout le dépôt. | `npx tsx scripts/probe-orchestrator-e2e.mts` → `ERR_MODULE_NOT_FOUND`. Le supprimer ne peut casser aucune exécution qui marche aujourd'hui. |
| Signal d'un livrable supprimé | `scripts/run-worktree-map-signal.ps1:14` | Rejoue le livrable « vue Worktrees — plan de métro ». Les **7** fichiers qu'il cite ont disparu : `src/shared/worktree-map.ts` + `.test.ts` (l.14, 26), `src/main/worktree-map-main.ts` (l.27), `WorktreeMapView.tsx/.css/.test.tsx/.style.test.ts` (l.15, 16, 28, 29). | 0 référence à `run-worktree-map-signal`. Dans tout le dépôt, `wtmap` n'apparaît dans aucun fichier de `src/`, et `WorktreeMapView` non plus. | `powershell -File scripts/run-worktree-map-signal.ps1` → `exit 1` à la première suite (« No test files found »). Le signal ne peut plus jamais être vert. |
| Chemin absolu inexistant | `scripts/run-vitest-hooks.ps1:2` | `$wt = 'C:\Amitel\Autowin-OS-wt\hooks'` : ce worktree n'existe pas sur la machine. Avec `$ErrorActionPreference='Stop'` posé l.1, le `Set-Location` l.3 est fatal — le script meurt avant d'atteindre vitest. | 0 référence à `run-vitest-hooks`, ni en `.ps1` ni sans extension. | `Test-Path 'C:\Amitel\Autowin-OS-wt\hooks'` → `False`. Même forme pour `run-vitest-longwf.ps1:3` (`\long-workflows`), `run-vitest-sourcecontrol.ps1:3` (`\worktrees-in-chat`) et `run-vitest-scouttable.ps1:3` (`\scout-table`) : les 4 chemins sont absents. |
| Chemin absolu inexistant | `scripts/run-vitest-autoupdate.ps1:2` | `Set-Location -Path 'C:\Amitel\Autowin OS'` — l'ancien emplacement du dépôt, absent de la machine. Fatal l.2, avant tout appel de test. Le dépôt vit désormais sous `D:\AutoWinOS\…`. | 0 référence à `run-vitest-autoupdate`. Idem pour `run-vitest-secfix.ps1:2` et `run-vitest-turn-journal.ps1:2`, qui codent le même chemin. | `Test-Path 'C:\Amitel\Autowin OS'` → `False`. Aucun de ces 3 scripts ne peut atteindre son `vitest.cmd`. |
| Cible de test supprimée | `scripts/run-vitest-worktree.ps1:7` | Cible `src/main/store/worktree-manager.test.ts`, qui n'existe plus : le fichier a été éclaté en 17 fichiers `worktree-manager.<sujet>.test.ts`. Le nom exact cité n'est plus produit par aucun glob. | 0 référence à `run-vitest-worktree`. | `Test-Path src/main/store/worktree-manager.test.ts` → `False`, alors que `git ls-files 'src/main/store/*worktree-manager*'` rend 19 entrées, toutes suffixées. `vitest` sort non-zéro sur ce chemin. |
| Cible de test supprimée | `scripts/run-a2-workspace-hub-signal.ps1:22` | Deux des 14 cibles ont disparu : `src/main/store/worktree-manager.test.ts` (l.22) et `src/shared/worktree-activity-model.test.ts` (l.34, alors que la source `worktree-activity-model.ts` survit). `Invoke-Checked` (l.13) lève dès le premier code non-zéro. | 0 référence à `run-a2-workspace-hub-signal`. | Les deux `Test-Path` rendent `False`. Le bloc « Tests lifecycle A2 » échoue avant même d'atteindre le typecheck l.41 : le signal `A2_WORKSPACE_HUB_SIGNAL_OK` est inatteignable. |
| Cible de test supprimée | `scripts/run-vitest-scouttable.ps1:7` | Cible `src/renderer/src/components/ScoutTable.test.tsx` ; il ne reste que des `scout-table.*.test.ts` (minuscules, sans composant `.tsx`). Double panne : s'ajoute au worktree mort de la l.3. | 0 référence à `run-vitest-scouttable`. | `git ls-files` sur `ScoutTable` → aucun résultat ; seuls `scout-table.test.ts`, `scout-table.perimetre.test.ts`, `scout-table.ligne-tronquee.test.ts` et `scout-table.ts` existent. |
| Sélecteur renommé | `scripts/cdp-a2-hub-proof.mjs:143` | Clique `[data-testid="sc-view-worktree"]` pour basculer le panneau vers le hub. Ce testid n'existe plus : `SourceControlPane.tsx:384` expose désormais `sc-view-workspace`. Le clic optionnel (`worktree?.click()`) n'échoue pas, mais le panneau reste sur sa vue par défaut, donc `hubFound` est faux et la garde l.176-189 lève `Preuve A2 invalide`. | 0 référence à `cdp-a2-hub-proof`, ni depuis `verify-a2-hub-proof.ps1`, ni ailleurs. | `sc-view-worktree` est absent de tout `src/` ; les seuls `sc-view-*` présents sont `sc-view-workspace`. Les autres testids du script (`wt-view`, `wt-agent-office`, `wt-inbox`, `setWorktreeFixture`) existent encore — c'est bien ce seul sélecteur qui coupe la preuve. |
| Validateur d'un artefact non produit | `scripts/verify-a2-hub-proof.ps1:11` | Ne valide que les fichiers `a2-hub-340/420.json` + `.png`, produits uniquement par `cdp-a2-hub-proof.mjs` (l.196-199 de celui-ci). Ce producteur lève avant d'écrire (ligne précédente). Le validateur ne peut donc plus voir autre chose qu'un artefact périmé. | 0 référence à `verify-a2-hub-proof`. Ses deux paramètres sont `Mandatory` : rien ne peut l'appeler sans `-ProofRoot`/`-RunId`. | Il ferait `throw "Preuve JSON manquante"` l.14 sur toute racine propre. À retirer avec son producteur, pas avant. |
| Branche morte dans un fichier vivant | `scripts/measure-orchestration-tokens.mjs:84` | La boucle d'approbation appelle `window.api.authorityPending()` (l.84) puis `authorityResolve` (l.88). Ces deux méthodes n'existent nulle part dans `src/` — même famille supprimée que `src/main/authority/sas`. Le `try/catch` inline rend `[]` en silence : `approved` reste 0 pour les 160 itérations et le bloc l.86-93 est inatteignable. | Le fichier, lui, est **vivant** (cité en commentaire `src/main/orchestrator.ts:3578`) — c'est la branche qui est morte, pas le script. | `authorityPending` / `authorityResolve` : 0 occurrence dans `src/preload/` et 0 dans tout `src/`. Retirer les l.79-93 sans toucher au reste de la mesure. |
| Sélecteur supprimé | `scripts/cdp-dogfood-inspect.mjs:515` | Le bloc `verifyAgentStudio` clique `[data-testid="nav-agent-studio"]`, testid qui n'existe plus. La navigation expose `nav-chat`, `nav-worktree`, `nav-task-manager`, `nav-observatory`, `nav-settings`, `nav-knowledge`, `nav-candidates` — pas `nav-agent-studio`. | 0 référence à `cdp-dogfood-inspect`. | `nav-agent-studio` : 0 occurrence dans `src/`, alors que le mot `agent-studio` seul survit dans 12 fichiers (le concept est resté, le point d'entrée testable a disparu). |
| Bloc mort dans une sonde | `scripts/cdp-dogfood-inspect.mjs:572` | Le bloc d'inspection « Worktrees » lit `worktree-map-loading` (l.572-573, 588), `worktree-map-error` (l.580), `worktree-map-retry` (l.586), `worktree-map-error-pick` (l.587), `svg.wtmap-plan` (l.584) et `.wtmap-stats` (l.574) — toute la vue « plan de métro » supprimée, celle-là même que `run-worktree-map-signal.ps1` prétend encore prouver. La boucle l.577-591 tourne alors 30 fois à vide. | 0 référence à `cdp-dogfood-inspect`. | Aucun de ces 5 testids n'existe dans `src/`, et `wtmap` n'apparaît dans **aucun** fichier de `src/`. Le bloc peut partir même si on garde le reste de la sonde. |
| Doublon de variante abandonnée | `scripts/capture-printwindow.ps1:1` | Variante `PrintWindow` de `capture-window.ps1`, avec les mêmes paramètres (`-Title`/`-Out`/`-ProcessId`), la même sortie et le même défaut `$Out` pointant vers `C:\Amitel\Autowin OS\p0-capture.png` — un dossier qui n'existe plus. Le jumeau `capture-window.ps1`, lui, est documenté et utilisé. | 0 référence à `capture-printwindow`, avec ou sans extension, dans tout le dépôt. `capture-window` est cité par `skills/draft/SKILL.md:45`. | `Test-Path 'C:\Amitel\Autowin OS'` → `False` : appelé sans `-Out`, il lève à l'écriture l.30. Aucune capacité que `capture-window.ps1` n'ait déjà. |
| Sonde jetable, campagne close | `scripts/cdp-trois-conversations-proof.mjs:34` | Démonstration ponctuelle d'une demande datée (« lancer 3 convers sur la même chose »). Elle crée 3 conversations nommées en dur `preuve-1/2/3` et leur fait écrire dans `PREUVE-TROIS-CONVERS.md` à la racine — fichier jamais commité et absent du dépôt. Le script n'assure rien : il `console.log` puis sort. | 0 référence à `cdp-trois-conversations-proof`. | `Test-Path PREUVE-TROIS-CONVERS.md` → `False`. Aucune assertion, donc aucune régression détectable par son retrait. |
| Sonde jetable, campagne close | `scripts/cdp-trois-conversations-suivi.mjs:9` | Compagnon du précédent : `process.argv.slice(2)` attend les identifiants de conversation que `cdp-trois-conversations-proof.mjs` imprime sur `stdout` (l.40, `CONVERSATIONS […]`). Sans son producteur, `CIBLES` est vide et la boucle l.10-16 sort au premier tour. | 0 référence à `cdp-trois-conversations-suivi`. Aucun script ne l'enchaîne au producteur : le raccord était manuel. | Un lancement sans argument fait `act.length === 0`, donc 40 tours de 15 s sans jamais imprimer `TERMINES`. À retirer en même temps que le producteur. |

## Faux positifs écartés

Candidats sans appelant textuel, ouverts puis **rejetés** — ils sont vivants.

| Fichier | Raison du rejet |
|---|---|
| `scripts/arena-duel.test.mjs`, `arena-protocole-check.conv-126.test.mjs`, `cdp-device-metrics.test.mjs`, `cdp-proof-validation.test.mjs`, `cdp-verdict-collection.test.mjs`, `cdp-workflow-execution-graph.test.mjs`, `export-compute-fabric-context.test.mjs`, `pilote-prompts.test.mjs`, `scout-rendement.test.mjs`, `ui-capture-state.test.mjs` | Aucune citation nulle part, mais ils sont ramassés par le glob par défaut de vitest. `npx vitest list --filesOnly` les rend tous les 10, préfixés `[unite]`. Un `*.test.mjs` sous `scripts/` n'a pas besoin d'appelant. |
| `scripts/cdp-verdict.test.mjs` | Explicitement **exclu** de la suite (`vitest.config.ts:79`) — ce qui ressemble à un abandon. C'est l'inverse : `cdp-verdict-collection.test.mjs:4` l'importe pour prouver qu'il se collecte sans figer le worker. L'exclusion est le dispositif, pas la mise au rebut. |
| `scripts/outlook-local-snapshot.ps1`, `outlook-local-open.ps1`, `outlook-local-reply.ps1`, `outlook-local-marquer-lu.ps1` | Aucun appel depuis `src/`, mais `electron-builder.yml:26-29` les liste en `asarUnpack` avec un commentaire expliquant que PowerShell ne peut pas exécuter un fichier situé dans l'archive asar. Retrait = passerelle Outlook cassée en package uniquement. |
| `scripts/verify-brain-outcome-writeback.ps1` | Cité par `package.json:10` et `:33` comme `command` et `attests` d'un `autowin.learningOracles`. Consommé par configuration, jamais par import. |
| `scripts/capture-window.ps1` | Cité par `skills/draft/SKILL.md:45` et `:61`. Réserve : la skill le décrit avec `-Exe`/`-WindowTitle`/`-ProcId` et « PrintWindow », alors que le fichier réel expose `-TitleLike`/`-Out`/`-ProcessId` et fait un `CopyFromScreen`. La documentation a divergé du script, mais l'appelant existe — ce n'est pas du code mort, c'est un contrat à réaligner. |
| `scripts/scout-residus.mjs`, `scout-rendement.mjs`, `pilote-prompts.cli.mjs`, `arena-protocole-check.mjs`, `arena-duel.mjs`, `brain-doctor.mjs`, `export-compute-fabric-context.mjs`, `bootstrap-deps.ps1`, `clean-generated-artifacts.ps1` + `.test.ps1`, `worktree-fingerprint.test.ps1`, `launch-dev.test.ps1`, `launch_dev_phases_test.py`, `launch_dev_stdout_test.py`, `enregistreur_sortie_test.py`, `refresh-desktop-shortcut.ps1`, `create-dev-shortcut.ps1` | Tous câblés dans le bloc `scripts` de `package.json` (l.105-129). |
| `scripts/launch-dev.vbs`, `launch_dev_phases.py`, `launch_dev_splash.py`, `mutex_wait_scenario.py`, `mesure-pixels-allumes.py`, `monitor-common.ps1`, `cdp-device-metrics.mjs`, `cdp-proof-validation.mjs`, `cdp-verdict.mjs`, `cdp-model-catalog.mjs`, `cdp-hooks.mjs`, `assert-package-content.ps1`, `assert-ui-package-fresh.ps1`, `task-manager-proof-validator.mjs`, `verifier-candidats-veille.ts`, `desktop-control-live-probe.ts` | Invisibles depuis la racine, mais appelés depuis d'autres scripts (import, dot-source, `-File`, `subprocess`). `cdp-device-metrics.mjs` par exemple est importé par 9 sondes CDP. |
| `scripts/measure-orchestration-tokens.mjs` (le fichier) | Cité en commentaire par `src/main/orchestrator.ts:3578` comme outil de re-mesure. Seule sa boucle `authority` est morte (voir la table) ; le script reste l'outil de mesure désigné. |
| `scripts/run-vitest-conv57.ps1`, `run-vitest-scout-suggestions.ps1` | Même famille que les runners morts, et sans appelant — mais ils résolvent leur racine par `Join-Path $PSScriptRoot '..'` (portable, pas de chemin absolu) et **toutes** leurs cibles existent. Ils passent au vert tels quels. |
| `scripts/test-btw.ps1` | Suspect parce que son en-tête annonce « parseBtw + modèle Chat » alors qu'il lance `fanout-grouping`. Vérifié : `parseBtw` est bel et bien testé dans `src/renderer/src/components/fanout-grouping.test.ts`. La cible est juste. Il reste un sous-ensemble strict de `test-fanout-grid.ps1` — redondance, pas mort. |
| `scripts/verify-accueil.ps1`, `verify-deblocage.ps1`, `verify-erreurs-captees.ps1`, `verify-wire-all.ps1`, `verify-task-manager.ps1`, `run-categories-cerveau-signal.ps1`, `memory-system-checkpoint.ps1` | Sans appelant, mais chaque chemin `src/…` qu'ils citent existe encore — vérifié un par un. Ce sont des signaux rejouables à la main, verts aujourd'hui. `verify-wire-all.ps1:16` porte même sa propre garde d'existence. |
| `scripts/publier-isole.ps1` | Le balayage l'a signalé pour `src/main/a.ts` / `src/main/a.test.ts` inexistants (l.27). Faux positif : c'est le bloc `.EXAMPLE` de l'aide `comment-based help`, pas une cible. |
| `scripts/probe-conv-activity.mts`, `probe-conv-workflows.mts`, `probe-live-subagents.mts`, `probe-pilot-chat.mts`, `probe-skill-node-mcp.mts`, `probe-codex-orchestration-executor.mts`, `lire-calibration.ts`, `live-listing-proof.mts`, `conversation-delete-restart-probe.ts`, `lancer-veille.ts` | Zéro appelant, mais **tous** leurs imports relatifs vers `src/` résolvent. Seul `probe-orchestrator-e2e.mts` échoue (retenu dans la table). Une sonde manuelle qui charge encore n'est pas du résidu prouvé. |
| `scripts/assert-package-content.test.ps1` | Seul `*.test.ps1` sans script npm dédié, contrairement à `clean-generated`, `worktree-fingerprint` et `launch-dev`. Mais son sujet `assert-package-content.ps1` est vivant (appelé par `verify-memory-system.ps1`) et l'auto-test reste valide : c'est un câblage manquant, pas un test mort. |
| ~35 autres `scripts/cdp-*.mjs` sans appelant (`cdp-e2e-chat`, `cdp-graph-camera`, `cdp-skills-registry`, `cdp-shell-proof`, `cdp-flux-isole`, `cdp-markdown-overflow`, `cdp-rail-visible-proof`, `cdp-knowledge-*`, `cdp-image-*`, `cdp-sonde-*`, `cdp-monitor-10`, `cdp-juger-experience`, `cdp-wire-all-e2e`, …) | Écartés en bloc après contrôle : leurs `data-testid` et leurs `window.api.*` résolvent tous dans `src/`. L'absence d'appelant est ici la **norme du dépôt** — ce sont des preuves CDP lancées à la main contre l'app en marche. Le chemin absolu `C:/Amitel/Autowin OS/artifacts/…` qu'ils codent en dur les rend fragiles (voir la note de `ui-capture.mjs:219`, qui a corrigé exactement ce défaut chez lui), mais fragile n'est pas mort. |
